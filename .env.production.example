# === ClawCRM Deployment Configuration ===

# WebSocket URL to the OpenClaw server (must be reachable from the browser!)
VITE_WS_URL=wss://crm.example.com:3848

# Instance key for initial pairing (one-time, via /crm-admin generate-instance-key)
VITE_INSTANCE_KEY=ik_xxxxxxxxxxxx

# === OpenClaw Plugin ===
# Path to the SQLite database
CLAWCRM_DB_PATH=/data/clawcrm.db
# WebSocket port
CLAWCRM_WS_PORT=3848
# Allowed origins (comma-separated)
CLAWCRM_WS_ALLOWED_ORIGINS=https://crm.example.com
# Admin emails (comma-separated)
CLAWCRM_ADMIN_USERS=admin@example.com
